[Unit]
Description=kids-games GitHub webhook deployer
After=network.target

[Service]
Type=simple
EnvironmentFile=%h/.config/games-webhook.env
ExecStart=/usr/bin/python3 %h/games-site/deploy/webhook.py
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
