## 1. 固化路口缺陷

- [x] 1.1 为归一化双轴意图和方向迟滞增加失败单元测试，运行 `node --test tests/joystick-controls.test.mjs` 确认旧主轴实现无法保留次方向。
- [x] 1.2 用手工 L 形、T 字、十字、连续弯路和死路夹具增加失败集成测试，断言真实人物坐标与松手后零残留移动。

## 2. 实现路径感知控制

- [x] 2.1 实现按摇杆尺寸归一化的死区、主次方向和迟滞解析，运行摇杆单元测试验证临界抖动不翻转且不产生斜向跨格。
- [x] 2.2 增加只读物理可通行查询与路径感知导航状态，运行路口集成测试验证预转向、受阻备用方向、短时缓存和无自主选路。
- [x] 2.3 将普通虚拟摇杆接入路径感知导航，并让取消、死区、失焦和隐藏统一清理状态；运行现有连续移动和多指取消测试确认无回归。
- [x] 2.4 保持炸药、钩子、键盘和无障碍按钮走精确单方向路径，运行引擎与控制器测试确认工具只提交一次且不自动换向。

## 3. 跨设备回归与发布

- [x] 3.1 扩展真实浏览器手机和平板 pointer 测试，验证相同比例输入、L/T 路口转弯、松手急停、画布无遮挡和页面无选区。
- [x] 3.2 更新统一缓存版本并运行 `npm test`、`git diff --check` 与 `openspec validate improve-maze-junction-joystick --strict`；记录与本次无关的既有主题渲染时序波动，不放宽摇杆断言。
- [x] 3.3 审查差异后提交、推送并通过现有 PR 自动部署，在线上手机尺寸验证新版资源、路口转向和释放停止；任一门槛失败则停止发布。

## 4. 优化触控人体工学

- [x] 4.1 为立即单步、240 毫秒初始停顿、165 毫秒平缓重复、长按 1 秒后 125 毫秒加速以及方向改变重置节奏增加失败单元测试。
- [x] 4.2 实现可取消的分段重复调度，保持工具一次性输入和路口预转向语义不变。
- [x] 4.3 将手机竖屏摇杆扩大到至少 84 像素、平板扩大到至少 90 像素、极矮横屏保持至少 68 像素，并让底部摇杆卡片成为有效触控区且不遮挡画布。
- [x] 4.4 扩展手机、平板和桌面真实浏览器回归，验证实际尺寸、轻触单步、持续加速、路口转向、滑出后指针捕获及释放停止。
- [ ] 4.5 更新统一缓存版本，执行完整测试、OpenSpec 严格校验、差异审查、发布和生产域名多设备复验。
