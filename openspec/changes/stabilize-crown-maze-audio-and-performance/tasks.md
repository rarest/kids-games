## 1. 先建立失败回归测试

- [x] 1.1 扩展 `tests/audio.test.mjs` 的可注入 AudioContext、fetch 和媒体元素替身，覆盖首次手势解锁、解码排队、背景音乐并行、格式降级、节点上限、静音和后台恢复；先运行该文件并确认新增断言在旧实现上失败。
- [x] 1.2 为背景音乐增加时长、三个差异化段落、循环边界静音/音量跳变和兼容格式检查；运行对应资源测试并确认当前 24 秒资源无法通过。
- [x] 1.3 在页面结构与真实浏览器测试中加入手机、平板、鼠标长按拖动后的选区、长按菜单/拖拽保护和指针取消断言；运行相关测试并确认旧页面至少在选区保护断言上失败。
- [x] 1.4 为渲染器世界缓存失效条件、移动/桌面帧率门限、输入后 50 毫秒内绘制及诊断上限编写测试；运行相关测试并确认旧的逐帧模糊与无限制调度无法通过。

## 2. 修复背景音乐与操作音效

- [x] 2.1 重写皇家花园音乐生成脚本，生成约 72 秒 A/B/C 三段且首尾连续的流式音乐，并生成移动 Safari 兼容资源；使用音频分析测试和 `ffprobe` 验证时长、声道、编码、段落差异及循环接缝均达标。
- [x] 2.2 从现有真实录制效果音生成 MP3 兼容版本并更新来源台账，验证九类音效均非空、内容各异、总资源体积受控且公共资源引用存在。
- [x] 2.3 将短音效控制器实现为单 AudioContext、预取/预解码缓冲、400 毫秒有效排队、有限活跃节点和首次手势解锁的持久媒体元素降级，保留流式背景音乐；覆盖构造、恢复和解码失败并运行 `node --test tests/audio.test.mjs tests/sound-events.test.mjs` 验证全部音频事件及生命周期通过。
- [x] 2.4 把首次指针、音乐开关、页面可见性和诊断数据接入统一音频控制器；使用真实浏览器诊断确认背景音乐播放时脚步、碰壁、拾取和工具事件均产生独立效果节点，切后台不会补播旧事件。

## 3. 消除浏览器系统选区

- [x] 3.1 在游戏根节点、HUD、画布和控制区加入跨 Safari 的禁止选择、禁止长按菜单与禁止原生拖拽规则，并补齐指针取消/失焦清理；运行页面结构和手势单元测试验证样式与状态机。
- [x] 3.2 在 390×844 手机、平板和桌面浏览器配置下执行长按、跨区拖动、多指取消和重新开始手势，验证 `getSelection()` 为空、无原生拖拽且人物移动和按钮点击仍正常。
- [x] 3.3 为四向虚拟摇杆编写死区、主轴方向、立即首步、约 100 毫秒连续重复、松开/取消归中和工具单次方向测试，并确认旧画布手势实现无法通过。
- [x] 3.4 将底部手势说明卡替换为画布外虚拟摇杆，移除画布拖动移动绑定并接入手机、平板、鼠标、触控笔、键盘和无障碍输入；运行三设备浏览器测试确认画布无遮挡、摇杆可连续行走且无系统选区。

## 4. 降低持续渲染负载

- [x] 4.1 实现最多一张世界坐标墙体静态缓存，将三层模糊墙影、基础墙面和固定轮廓移出逐帧路径，并提供记忆失败键的创建失败回退；运行渲染单元测试验证普通移动命中缓存，炸墙、换关、旋转和 DPR 变化正确重建，相同失败键只尝试一次且缓存数量始终不超过一。
- [x] 4.2 将动画循环改为仅在游戏可见时运行的失效驱动调度，移动设备硬上限 30 FPS、桌面硬上限 60 FPS，并在输入后 50 毫秒内请求下一帧；通过虚拟时钟测试验证持续失效的帧数门限、定时唤醒和隐藏页面零持续绘制。
- [x] 4.3 扩展诊断模式以报告计划帧、实际绘制、缓存命中/重建/降级、活跃音频节点和动态对象数量，生产模式关闭逐帧测试签名分配；运行诊断测试验证字段只读且所有计数有硬上限。
- [x] 4.4 运行全部 19 关手机主题巡检和等效 10 分钟重复负载，验证粒子、拖尾、环境角色、音频节点、缓存及 JS 堆不持续增长，末段响应不显著劣化，并记录优化前后的主线程任务占比。
- [x] 4.5 增加持续失效和静态缓存创建失败回归，确保手机连续输入仍不超过 30 FPS、空闲不持续申请浏览器帧，且同一失败缓存键只尝试一次。

## 5. 集成、发布与线上验收

- [x] 5.1 更新 HTML、ES 模块和音频 URL 的统一缓存版本并检查所有新增音频公共路径，运行 `npm test`、`openspec validate stabilize-crown-maze-audio-and-performance --strict` 和生产同步排除测试，确认完整回归套件零失败且 OpenSpec 元数据不会被部署。
- [x] 5.2 审查最终差异，确认没有临时诊断入口、调试日志、未声明依赖或存档/关卡规则变化；通过 `git diff --check`、资源清单测试和变更规格逐项核对。
- [ ] 5.3 提交并通过现有 PR/自动部署流程发布，等待部署完成后在 `games.596996.xyz` 验证新版资源版本、背景音乐跨循环、操作音效、手机/平板无选区及性能诊断；如任一发布门槛失败则停止上线并按设计中的回滚步骤恢复。
